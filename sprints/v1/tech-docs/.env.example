# Voice2Code - Environment Variables Template
# Sprint v1

# =============================================================================
# IMPORTANT: Voice2Code is a client-side VS Code extension with NO backend
# These environment variables are for DEVELOPMENT/TESTING only
# =============================================================================

# STT Model Endpoint (for testing)
# User configures this in VS Code settings, not here
VOICE2CODE_TEST_ENDPOINT_URL=http://localhost:11434/api/generate
VOICE2CODE_TEST_MODEL_NAME=whisper-large-v3

# API Key (for testing cloud endpoints)
# In production, users store this in VS Code SecretStorage
# NEVER commit real API keys
VOICE2CODE_TEST_API_KEY=sk-test-key-here

# Development Settings
NODE_ENV=development
DEBUG=true

# Testing Configuration
# Set to 'true' to use mock STT server in tests
USE_MOCK_STT_SERVER=true
MOCK_SERVER_PORT=8080

# Audio Testing
# Path to test audio files
TEST_AUDIO_DIR=./tests/fixtures/audio-samples

# Logging
# Set log level for development (error, warn, info, debug)
LOG_LEVEL=debug

# VS Code Extension Development
# Extension host debugging port
EXT_HOST_DEBUG_PORT=5858

# =============================================================================
# NOTES:
# - These variables are ONLY for local development and testing
# - Production users configure everything via VS Code settings UI
# - Secrets are stored in VS Code SecretStorage (OS keychain), not env vars
# - Do NOT use .env files in production (extension doesn't read them)
# =============================================================================
